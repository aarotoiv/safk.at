<html>
    <head>
        <meta name="viewport" content="width=device-width,initial-scale=1.0">
        <title>
            source.
        </title>
    </head>
    <body>
        <style>
            @import url('https://fonts.googleapis.com/css?family=Roboto&display=swap');
            body {
                background: #000;
                font-family: 'Roboto Mono',monospace;
                font-weight: normal;
                font-style: normal; 
                color: #fff;
            }
            .door-text {
                margin: 0;
                font-size: 25px;
                text-align: center;
                position: relative;
            }
            .door-text:hover:after {
                visibility: visible;
                opacity: 1;
                transform: translateY(-10px);
            }
            .door-text:after {
                content: "or is it...";
                position: absolute;
                left: 0;
                bottom: -40px;
                width: 100%;
                font-size: 15px;
                visibility: hidden;
                transition: 0.3s ease-in-out;
                opacity: 0;
                transform: translateY(0px);
            }
            .door {
                width: 250px;
                height: 400px;
                background: #000;
                border: solid 1px #fff;
                position: relative;
                overflow: hidden;
                background-image: url("https://sourcery.rocks/static/96869fdd86c7496a23c7a2bab50849c0-038dc49b5c3821901578f1188d9f5dce.jpeg");
                background-size: cover;
                background-position: center center;
                margin: 50px auto;
            }
            .door-inner {
                position: absolute;
                z-index: 1;
                width: 100%;
                height: 100%;
                border: solid 1px #fff;
                background: #000;
            }
            .door-inner:after {
                content: " ";
                position: absolute;
                right: 30px;
                top: 50%;
                background: #000;
                border: solid 1px #fff;
                width: 20px;
                height: 20px;
            }
            .door.open .door-inner {
                animation: doorOpenAnim 0.7s ease-in-out forwards;
            }
            .alpha {
                position: absolute;
                bottom: 0;
                font-size: 10px;
                text-align: center;
            }
            @keyframes doorOpenAnim {
                from {
                    transform: translateX(0px);
                }
                to { 
                    transform: translateX(-170px);
                }
            }
        </style>
        
        <p class="alpha">This feature is still in an early alpha phase.</p>
        <% if(isDoorOpen) { %>
            <div class="door open">
                <div class="door-inner"></div>
            </div>
            <p class="door-text">Door is open</p>
        <% } else { %>
            <div class="door">
                <div class="door-inner"></div>
            </div>
            <p class="door-text">Door is closed</p>
        <% } %>
        
    </body>
</html>